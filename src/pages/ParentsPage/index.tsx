import React from 'react';
import { Outlet } from 'react-router-dom';

const ParentsPage = () => {
  return (
    <div>
      ParentsPage
      <Outlet />
    </div>
  );
};

export default ParentsPage;
